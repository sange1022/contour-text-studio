# 轮廓海报工具提示词

> 请制作一款名为「轮廓文字」的静态海报生成工具。用户可以上传 1—5 张 PNG、JPG 或 WebP 图片，工具需要根据当前图片像素识别主体的外轮廓与内部孔洞，并让文字形状沿真实轮廓排列。
>
> 提供阈值、平滑程度、最小识别面积和前景反转调节；上传原图默认铺满画布作为海报背景，并可独立关闭；允许分别显示识别主体、外轮廓文字和孔洞文字。文字支持自定义内容、字号、密度、扩散程度和字体上传，底形可选择正方形、圆形或三角形，并可切换为空心样式。主体、背景、文字、形状填充及边框颜色均可调整。
>
> 提供「轮廓、圆化、粒子、网格、回吸」五种静态海报样式和可编辑海报模板。加入连续原图的局部网格变形：用户自定义行列数，点击选择某一格，只在该格原始边界内调整位移、横纵拉伸、倾斜和模糊，未选择区域必须保持连续且不发生变化。每格另有数据故障、数字切片、CRT 扫描、全息偏色、信号波纹、液态融化、玻璃折射、热成像、X 光反相与数码网点十种一键科技感特效，并提供特效强度调节。画布可使用原图比例、社交媒体常用比例以及 A3 / A4 印刷尺寸，最终仅导出 PNG 或 SVG 海报，并支持透明背景与 2× 高清导出。
>
> 界面采用简洁的白底黑线参数面板与大尺寸海报画布。左侧以窄工具栏切换素材、模板、变形、轮廓与文字五个独立工作区，每次只显示当前任务需要的参数，切换时画布保持可见；局部变形以“选格—选效果—调强度”为主流程，位移、拉伸、倾斜与模糊默认收进可展开的精细调整。

## 当前范围

- 只处理静态图片；
- 只生成静态海报；
- 只导出 PNG 与 SVG；
- 不包含任何动态媒体工作流。
